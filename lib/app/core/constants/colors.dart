import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color error = Colors.red;

  // Light mode ------------------------------------------------

  static const Color light = Color.fromARGB(255, 255, 235, 205);
  static const Color lightSec = Colors.black;
  static const Color lightBg = Colors.white;

  // Dark mode ------------------------------------------------

  static const Color dark = Colors.black;
  static const Color darkSec = Colors.white;
  static const Color darkBg = Color.fromARGB(255, 100, 100, 100);
}
